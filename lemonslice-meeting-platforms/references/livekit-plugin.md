# LiveKit plugin preference

Inspect the installed LemonSlice LiveKit plugin package and current documentation. Prefer a supported plugin method over direct join/leave REST because it can coordinate the LiveKit session, broadcast publication, and lifecycle more safely.

If the installed version lacks meeting support, report that fact and use direct endpoints only when required.
