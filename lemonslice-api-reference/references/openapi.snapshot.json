{
  "audited_at": "2026-07-17",
  "generated_from": "https://lemonslice.com/docs/openapi.json",
  "notes": [
    "The normalizer unions required keys across OpenAPI oneOf/anyOf alternatives. agent_id and agent_image_url are alternative JSON selectors, not fields that should be sent together.",
    "The current self-managed schema exposes model, aspect_ratio, simulcast, and recording configuration; implementations must still validate the current endpoint and installed plugin separately.",
    "The control operation currently declares security: [] and response codes 200, 400, 404, 413, and 500. Rendered examples omit X-API-Key and do not show 413, so control remains a documented source conflict."
  ],
  "operations": {
    "POST /liveai/sessions": {
      "request": {
        "enums": {
          "aspect_ratio": [
            "1x1",
            "2x3",
            "9x16"
          ],
          "model": [
            null,
            "flash",
            "lite",
            "pro"
          ],
          "properties.enable_recording": [
            "cloud",
            "cloud-audio-only",
            "local",
            "raw-tracks"
          ],
          "transport_type": [
            "daily",
            "livekit"
          ]
        },
        "required": [
          "agent_id",
          "agent_image_url",
          "transport_type"
        ]
      },
      "responses": [
        "201",
        "400",
        "401",
        "402",
        "403",
        "404",
        "500"
      ],
      "security": [
        "X-API-Key"
      ]
    },
    "POST /liveai/sessions/{session_id}/control": {
      "request": {
        "required": [
          "agent_prompt",
          "event",
          "idle_prompt",
          "pose_trigger"
        ]
      },
      "responses": [
        "200",
        "400",
        "404",
        "413",
        "500"
      ],
      "security": []
    },
    "POST /liveai/sessions/{session_id}/join-meeting": {
      "request": {
        "required": [
          "broadcast_token",
          "livekit_url",
          "meeting_url"
        ]
      },
      "responses": [
        "200",
        "400",
        "401",
        "500",
        "502",
        "503",
        "504"
      ],
      "security": [
        "X-API-Key"
      ]
    }
  },
  "paths": [
    "/liveai/rooms",
    "/liveai/rooms/{session_id}",
    "/liveai/sessions",
    "/liveai/sessions/{session_id}",
    "/liveai/sessions/{session_id}/control",
    "/liveai/sessions/{session_id}/join-meeting",
    "/liveai/sessions/{session_id}/leave-meeting"
  ]
}
